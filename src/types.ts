export interface UserType {
  id: number,
  email: string
}

export interface Address {
  street: string,
  city: string,
  number: string,
  zip_code: string,
  country: string
  state: string
}
